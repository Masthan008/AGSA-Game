import type { TopicTheory } from './theoryData';

const lesson = (
  topicKey: string, title: string, category: string, overview: string, definition: string,
  keyProperties: string[], steps: string[], applications: string[], edgeCases: string[],
  time: string, space: string,
): TopicTheory => ({
  topicKey, title, category, overview, definition, keyProperties,
  timeComplexities: [{ operation: 'Core operation', best: time, average: time, worst: time }],
  spaceComplexity: space,
  pinToPinSteps: steps.map((explanation, index) => ({ stepNumber: index + 1, title: `Step ${index + 1}`, explanation })),
  realWorldApplications: applications,
  edgeCases,
});

export const ADVANCED_THEORY_EXPANSION: Record<string, TopicTheory> = {
  toposort: lesson('toposort', 'Topological Sorting', 'Graphs', 'Orders dependent work so every prerequisite appears first.', 'A topological order is a linear ordering of a directed acyclic graph (DAG) where every edge u → v places u before v.', ['Only DAGs have a valid order.', 'Kahn uses indegrees; DFS uses finish times.', 'Multiple correct orders may exist.'], ['Compute every vertex indegree.', 'Queue all zero-indegree vertices.', 'Remove one, append it, and decrement its neighbours.', 'If fewer than V vertices are emitted, a cycle exists.'], ['Build systems', 'Course planning', 'Workflow scheduling'], ['Empty graph', 'Disconnected DAG', 'Self-loop or directed cycle'], 'O(V + E)', 'O(V)'),
  hashing: lesson('hashing', 'Hash Tables', 'Advanced Sets', 'Turns keys into bucket positions for near-constant-time lookup.', 'A hash function maps a key into a fixed bucket range; collision resolution preserves multiple keys that map to the same bucket.', ['Equal keys must hash equally.', 'Load factor controls resize pressure.', 'Chaining and open addressing resolve collisions.'], ['Hash the key into a bucket index.', 'Probe or traverse the bucket to compare actual keys.', 'Insert/update the matching entry.', 'Resize and rehash when the load threshold is crossed.'], ['Caches', 'Symbol tables', 'Database hash indexes'], ['Adversarial collisions', 'Deletion tombstones', 'Mutable keys'], 'O(1) average; O(N) worst', 'O(N)'),
  fenwick: lesson('fenwick', 'Fenwick Tree', 'Trees', 'Maintains prefix aggregates with a compact one-indexed array.', 'Each index stores the sum of a range determined by its least-significant set bit.', ['Use one-based indexing.', 'lowbit(i) = i & -i.', 'Point update and prefix query are logarithmic.'], ['For update, add delta at index i.', 'Move upward with i += lowbit(i).', 'For query, accumulate tree[i].', 'Move toward zero with i -= lowbit(i).'], ['Live frequency tables', 'Inversion counting', 'Range-sum dashboards'], ['Index zero loops forever', 'Overflow in large sums', 'Range query requires prefix(r)-prefix(l-1)'], 'O(log N)', 'O(N)'),
  editdistance: lesson('editdistance', 'Edit Distance', 'Dynamic Programming', 'Measures the minimum edits needed to transform one string into another.', 'Levenshtein distance permits insertion, deletion, and substitution, each normally costing one.', ['Rows/columns represent prefixes.', 'Matching characters carry the diagonal value.', 'Different characters take 1 + minimum of three neighbours.'], ['Initialize empty-prefix base cases.', 'Visit cells row by row.', 'Use diagonal on a match; otherwise choose insert/delete/replace.', 'Backtrack from the final cell to explain the edits.'], ['Spell checking', 'DNA comparison', 'Fuzzy search'], ['Empty strings', 'Unicode graphemes', 'Weighted edit costs'], 'O(MN)', 'O(MN), reducible to O(min(M,N))'),
  bitmaskdp: lesson('bitmaskdp', 'Bitmask Dynamic Programming', 'Dynamic Programming', 'Encodes a small chosen-set state inside integer bits.', 'A mask bit indicates whether an item is present; DP combines the mask with a position or endpoint.', ['Best when N is small, typically ≤ 20.', 'Set bit with mask | (1<<i).', 'Test bit with mask & (1<<i).'], ['Define exactly what each mask means.', 'Choose a base mask.', 'Try every item not yet selected.', 'Memoize or fill masks and read the full-mask answer.'], ['Travelling Salesperson', 'Assignment matching', 'Subset optimization'], ['Exponential state count', 'Shift overflow', 'Ambiguous state definition'], 'O(2^N · poly(N))', 'O(2^N · poly(N))'),
  maxflow: lesson('maxflow', 'Maximum Flow', 'Graphs', 'Finds the greatest feasible flow from a source to a sink.', 'Flow respects edge capacity, conservation at intermediate vertices, and skew symmetry through a residual graph.', ['Residual reverse edges allow correction.', 'An augmenting path increases total flow.', 'Max-flow equals min-cut capacity.'], ['Initialize all flow to zero.', 'Find a source-to-sink path with residual capacity.', 'Take the minimum residual capacity on that path.', 'Augment forward edges, update reverse edges, and repeat.'], ['Network routing', 'Bipartite matching', 'Resource allocation'], ['Parallel edges', 'Disconnected sink', 'Integer overflow in total flow'], 'O(VE²) with Edmonds–Karp', 'O(V + E)'),
  skiplist: lesson('skiplist', 'Skip List', 'Advanced Sets', 'Uses randomized express lanes over a sorted linked list.', 'Each promoted level skips across multiple lower-level nodes, giving expected logarithmic search and updates.', ['Level 0 contains every key.', 'Higher levels contain random subsets.', 'Performance is expected, not worst-case guaranteed.'], ['Start at the highest sentinel.', 'Move right while the next key is smaller.', 'Drop one level when moving right would overshoot.', 'At level 0, inspect or splice the target.'], ['In-memory indexes', 'Concurrent ordered maps', 'Redis sorted structures'], ['Duplicate-key policy', 'Unlucky tall/flat structure', 'Random generator reproducibility'], 'O(log N) expected', 'O(N) expected'),
  splay: lesson('splay', 'Splay Tree', 'Trees', 'Moves every accessed key to the root so frequently used data stays close.', 'A splay tree is a BST that performs zig, zig-zig, or zig-zag rotations after access.', ['No stored balance metadata.', 'Individual operations can be linear.', 'Amortized operation cost is logarithmic.'], ['Perform a normal BST search/update.', 'Identify node, parent, and grandparent.', 'Apply zig, zig-zig, or zig-zag.', 'Repeat until the accessed node becomes root.'], ['Caches with locality', 'Ropes', 'Network routing tables'], ['Missing-key splay policy', 'Long single-operation latency', 'Rotation parent links'], 'O(log N) amortized', 'O(N)'),
  radixsort: lesson('radixsort', 'Radix Sort', 'Advanced Sets', 'Sorts keys digit by digit without comparing whole keys.', 'Stable counting/bucketing passes process digits from least to most significant (LSD) or recursively from most significant.', ['Every LSD pass must be stable.', 'Runtime depends on digit count and radix.', 'Negative values need an explicit policy.'], ['Choose a radix and digit order.', 'Place keys into digit buckets stably.', 'Collect buckets in digit order.', 'Repeat for every digit position.'], ['Integer sorting', 'Fixed-length identifiers', 'Lexicographic pipelines'], ['Negative numbers', 'Variable-length strings', 'Huge radix memory'], 'O(D(N + K))', 'O(N + K)'),
  zalgo: lesson('zalgo', 'Z Algorithm', 'Strings', 'Computes how much of the string prefix matches at every position.', 'Z[i] is the length of the longest substring starting at i that equals the string prefix.', ['Maintains a rightmost matching window [L,R].', 'Values inside the window reuse earlier work.', 'Each character extends R only O(1) amortized times.'], ['Initialize L=R=0.', 'Reuse Z[i-L] when i lies inside the window.', 'Compare beyond the known boundary.', 'Update [L,R] when the match extends farther.'], ['Pattern matching', 'String periodicity', 'Prefix occurrence counting'], ['Empty input', 'Separator collision in pattern$text', 'Definition of Z[0]'], 'O(N)', 'O(N)'),
  manacher: lesson('manacher', 'Manacher’s Algorithm', 'Strings', 'Finds all palindrome radii in linear time.', 'After inserting separators, P[i] stores the palindrome radius around each transformed-string center.', ['Mirror radii reuse known symmetry.', 'A center/right boundary tracks the farthest palindrome.', 'Transformation unifies odd and even lengths.'], ['Transform the string with separators and sentinels.', 'Seed a radius from the mirror when inside the boundary.', 'Expand while mirrored characters match.', 'Update center/right and remember the largest radius.'], ['Longest palindromic substring', 'Palindrome analytics', 'DNA symmetry scans'], ['Empty string', 'Sentinel collision', 'Mapping transformed indexes back'], 'O(N)', 'O(N)'),
  bloomfilter: lesson('bloomfilter', 'Bloom Filter', 'Advanced Sets', 'Answers “definitely absent” or “possibly present” with tiny memory.', 'Multiple hash functions set bits during insertion; lookup requires every corresponding bit to be set.', ['False positives are possible.', 'False negatives are not possible without deletion mistakes.', 'Capacity and hash count determine error rate.'], ['Hash the item k ways.', 'Set all k bit positions on insert.', 'Check all k positions on lookup.', 'If any bit is zero, the item is definitely absent.'], ['Cache filtering', 'Web-crawl deduplication', 'Database read avoidance'], ['Overfilled filter', 'Unsafe deletion', 'Poorly independent hashes'], 'O(K)', 'O(M) bits'),
  sparsetable: lesson('sparsetable', 'Sparse Table', 'Advanced Sets', 'Precomputes answers for immutable range queries.', 'table[k][i] stores the aggregate for the length-2^k interval beginning at i.', ['Best for idempotent operations such as min/max/GCD.', 'Updates require rebuilding.', 'Two overlapping blocks answer RMQ.'], ['Store all length-1 intervals.', 'Build powers of two from two half intervals.', 'For query length L, compute k=floor(log2 L).', 'Combine blocks starting at l and ending at r.'], ['Static RMQ', 'LCA preprocessing', 'Immutable analytics'], ['Empty range', 'Mutable input', 'Non-idempotent overlap such as sum'], 'O(1) query after O(N log N) build', 'O(N log N)'),
  nqueens: lesson('nqueens', 'N-Queens Backtracking', 'Dynamic Programming', 'Places queens so no two attack each other.', 'One queen is chosen per row while columns and both diagonal families remain unused.', ['Backtracking explores and reverses choices.', 'Columns and diagonals give O(1) safety checks.', 'Symmetry can reduce equivalent searches.'], ['Start at row zero.', 'Try each safe column.', 'Mark column and diagonals, then recurse.', 'On failure, unmark exactly those choices and try the next column.'], ['Constraint solving', 'Search pruning lessons', 'Scheduling analogies'], ['N=1 has one solution', 'N=2 and N=3 have none', 'Incorrect diagonal indexing'], 'O(N!) upper bound', 'O(N)'),
};
